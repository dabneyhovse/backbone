const db = require("../db");
const User = require("./user");
const Verification = require("./verification");
const Affilation = require("./affilation");

module.exports = {
  User,
  Verification,
  Affilation,
};
